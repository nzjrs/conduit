SUBDIRS = backpack-1.1

conduit_handlersdir = $(libdir)/conduit/modules/BackpackModule
conduit_handlers_PYTHON = BackpackModule.py

conduit_handlers_DATA = config.glade
EXTRA_DIST = config.glade

clean-local:
	rm -rf *.pyc *.pyo
