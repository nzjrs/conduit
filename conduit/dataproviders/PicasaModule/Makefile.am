SUBDIRS = PicasaAPI

conduit_handlersdir = $(libdir)/conduit/dataproviders/PicasaModule
conduit_handlers_PYTHON = PicasaModule.py

conduit_handlers_DATA = config.glade
EXTRA_DIST = config.glade

clean-local:
	rm -rf *.pyc *.pyo
