conduit_handlersdir = $(libdir)/conduit/dataproviders/FileModule
conduit_handlers_PYTHON = FileModule.py

conduit_handlers_DATA = config.glade
EXTRA_DIST = config.glade

clean-local:
	rm -rf *.pyc *.pyo
